# Credentials — never commit
.env

# Playwright persistent browser profile (contains session cookies)
.midas-session/
.midas-state*.json*
.midas-orders.log.jsonl

# Build & deps
node_modules/
dist/

# Discovery output (may contain account data)
discovery/

# Scan output — belongs to a separate project, kept out of this repo
scans/
*-log.txt
